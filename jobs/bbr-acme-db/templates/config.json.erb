{
  "username":"<%= p('database.user') %>",
  "password":"<%= p('database.password') %>",
  "host":"<%= p('database.host') %>",
  "port":<%= p('database.port') %>,
  "database":"<%= p('database.name') %>",
  "adapter":"<%= p('database.scheme') %>"
}
